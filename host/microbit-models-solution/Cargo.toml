[package]
name = "microbit-models-solution"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"], default-features = false }
defmt = { version = "1", optional = true }
arbitrary-int = "2"
